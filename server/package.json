{
  "name": "mini-map-server",
  "version": "0.0.2",
  "description": "A map server that allows you sharing map data",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "babel src --out-dir dist",
    "start": "node dist/server.js",
    "clean": "rm -rf dist/*",
    "debug": "DEBUG=server babel-node src/server.js",
    "watch": "babel --watch src --out-dir dist"
  },
  "author": "dimotsai",
  "license": "MIT",
  "dependencies": {
    "babel": "^5.6.14",
    "lodash": "^4.17.11",
    "msgpack": "^1.0.2",
    "object-assign": "^3.0.0",
    "ws": "^3.3.1"
  },
  "devDependencies": {
    "debug": "^2.2.0"
  }
}
